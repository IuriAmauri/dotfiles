# History Settings
HISTSIZE="${HISTSIZE}"
SAVEHIST="${HISTSIZE}"
HISTFILE=~/.zsh_history

setopt APPEND_HISTORY
setopt HIST_IGNORE_ALL_DUPS
setopt HIST_REDUCE_BLANKS

# Initialize completions
autoload -Uz compinit && compinit

# Mise compatibility (Replaces set +h)
unhash -m '*' 2>/dev/null
